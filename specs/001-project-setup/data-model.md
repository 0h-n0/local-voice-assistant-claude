# Data Model: Project Setup - Backend & Frontend Foundation

**Date**: 2025-12-25
**Feature**: 001-project-setup

## Overview

このフェーズでは最小限のデータモデルのみを定義します。主にヘルスチェックAPIのレスポンスモデルです。

## Entities

### HealthResponse

ヘルスチェックエンドポイントのレスポンスを表すモデル。

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| status | string (enum) | Yes | サービス状態: "healthy" or "unhealthy" |
| version | string | Yes | アプリケーションバージョン（例: "0.1.0"） |
| timestamp | datetime | Yes | レスポンス生成時刻（ISO 8601形式） |

**Pydantic Model (Backend)**:
```python
from datetime import datetime
from typing import Literal

from pydantic import BaseModel


class HealthResponse(BaseModel):
    """Health check response model."""

    status: Literal["healthy", "unhealthy"]
    version: str
    timestamp: datetime
```

**TypeScript Type (Frontend)**:
```typescript
interface HealthResponse {
  status: "healthy" | "unhealthy";
  version: string;
  timestamp: string; // ISO 8601 format
}
```

## Relationships

現時点ではエンティティ間の関係はありません（単一エンティティのみ）。

## Validation Rules

| Entity | Field | Rule |
|--------|-------|------|
| HealthResponse | status | "healthy" または "unhealthy" のみ許可 |
| HealthResponse | version | 空文字列不可 |
| HealthResponse | timestamp | 有効なISO 8601形式の日時 |

## State Transitions

HealthResponseは状態を持たない読み取り専用モデルです。

---

*Data model generated by `/speckit.plan` Phase 1*
